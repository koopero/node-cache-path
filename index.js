const
  // bobject = require('bobject'),
  path = require('path')
;
function NodeCachePath ( opt ) {
  
  function cachePath( file ) {
    const 
      absFile = path.resolve( file )
    ;


    if ( baseDir ) {
      if ( )
    }
  }
}